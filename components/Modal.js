import React from "react";
import styled from "styled-components";

const ModalSty = styled.div`
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100vh;
`;

function Modal({ message }) {
  return <ModalSty>a</ModalSty>;
}

export default Modal;
